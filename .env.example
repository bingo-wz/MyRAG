# 数据库
DATABASE_USERNAME=myrag
DATABASE_PASSWORD=change-me-database

# MinIO
MINIO_ACCESS_KEY=myrag-admin
MINIO_SECRET_KEY=change-me-minio-secret
MINIO_BUCKET=myrag-knowledge

# Milvus
MILVUS_TOKEN=root:Milvus
MILVUS_COLLECTION=myrag_chunks_v1

# OpenAI-compatible Embedding 服务，例如 BGE-M3 推理网关
EMBEDDING_BASE_URL=http://host.docker.internal:11434/v1
EMBEDDING_API_KEY=
EMBEDDING_MODEL=bge-m3
EMBEDDING_DIMENSIONS=1024
EMBEDDING_BATCH_SIZE=32

# OpenAI-compatible Chat Completion 服务；可与 Embedding 共用网关
CHAT_BASE_URL=http://host.docker.internal:11434/v1
CHAT_API_KEY=
CHAT_MODEL=qwen3:8b
CHAT_MAX_TOKENS=800
CHAT_MAX_ATTEMPTS=2
AI_HTTP_CONNECT_TIMEOUT_SECONDS=3
AI_HTTP_READ_TIMEOUT_SECONDS=60

# OIDC/OAuth2 JWT；生产环境必须替换为真实身份提供方
OAUTH2_ISSUER_URI=https://id.example.com/realms/myrag
OAUTH2_AUDIENCE=myrag-api
OIDC_BROWSER_AUTHORITY=https://id.example.com/realms/myrag
OIDC_CLIENT_ID=myrag-console
OIDC_SCOPES=myrag-api

# 文件解析硬超时
PARSE_TIMEOUT_SECONDS=120

# Milvus 索引每日自动对账；可通过管理员接口手工触发
VECTOR_RECONCILIATION_ENABLED=true
VECTOR_RECONCILIATION_CRON=0 30 3 * * *

# 单机轻量环境保持 1；连接三节点以上 Kafka 集群时改为 3
KAFKA_REPLICATION_FACTOR=1
KAFKA_CONCURRENCY=1
KAFKA_IMPORT_PARTITIONS=3
OUTBOX_RETENTION_DAYS=7

# 中国大陆网络构建 OCR 镜像较慢时可取消注释
# UBUNTU_MIRROR=https://mirrors.tuna.tsinghua.edu.cn/ubuntu-ports
# MAVEN_MIRROR=https://maven.aliyun.com/repository/public
